{
  "name": "jupyter-iframe-commands-ui-tests",
  "version": "1.0.0",
  "description": "JupyterLab jupyter-iframe-commands Integration Tests",
  "private": true,
  "scripts": {
    "start": "python -m http.server -d ../demo -b 127.0.0.1 8080 && jupyter lab --config jupyter_server_test_config.py",
    "test": "jlpm playwright test",
    "test:update": "jlpm playwright test --update-snapshots"
  },
  "devDependencies": {
    "@jupyterlab/galata": "^5.0.5",
    "@playwright/test": "^1.37.0"
  }
}
